<aspectj>

    <aspects>
        <concrete-aspect name="ru.taskurotta.ru.taskurotta.test.monkey.MonkeyAspect"
                         extends="ru.taskurotta.ru.taskurotta.test.monkey.AbstractMonkeyAspect">
            <pointcut name="pointCut"
                      expression="execution(* ru.taskurotta.backend.queue.MemoryQueueBackend.pollCommit(..))"/>

        </concrete-aspect>

        <include within="ru.taskurotta..*"/>
    </aspects>

    <weaver options="-verbose -showWeaveInfo">
        <include within="ru.taskurotta.backend.queue.MemoryQueueBackend"/>
    </weaver>

</aspectj>

