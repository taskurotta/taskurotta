#Location of Spring application context:
#Hazelcast without mongo mapstore
contextLocation: "classpath*:spring/hz-ora-mongo.xml"

#Used for static resource serving in development mode
assets:
  overrides:
    /: ../dropwizard/src/main/resources/assets/

#Configuration of actors served by this server:
actorConfig:
  actorPreferences:
    - id: "default"

#Task server common properties (will be available as placeholders in spring context):
properties:
  pollDelay: 10
  pollDelayUnit: "SECONDS"
  hz.mancenter.enabled: true
  mongo.host: localhost
  mongo.port: 27017
#  oradb.url: "jdbc:oracle:thin:@172.16.4.240:1521:pgudb"
#  oradb.user: "rip_rr"
#  oradb.password: "rip_rr"
  oradb.url: "jdbc:oracle:thin:@(DESCRIPTION = (ADDRESS = (PROTOCOL = TCP)(HOST = db1.fccland.ru)(PORT = 1521))(CONNECT_DATA = (SERVER = DEDICATED)(SERVICE_NAME = orcl.fccland.ru)))"
  oradb.user: "taskurotta"
  oradb.password: "taskurotta"
  recovery.startup.run: false
  hz.queue.restore: true
  recovery.process.cron: "0 0 0/8 * * ?"
  gc.time-before-delete: "15 minutes"
  recovery.process.incomplete-timeout: "15 minutes"
  hz.nodes: "localhost:7777,localhost:7778"

#Http server configuration:
logging:
    level: WARN
    loggers:
        com.hazelcast: INFO
        ru.taskurotta: INFO
        com.yammer: INFO
        ru.taskurotta.hazelcast.HzQueueConfigSupport: DEBUG
#        ru.taskurotta.assemble: DEBUG
#        ru.taskurotta.hazelcast.queue.delay: DEBUG
#        ru.taskurotta.service.metrics.handler: DEBUG
#        ru.taskurotta.service.gc: DEBUG
#        ru.taskurotta.service.recovery: DEBUG
#        ru.taskurotta.server: DEBUG
#        ru.taskurotta.service.hz.queue: DEBUG
#        ru.taskurotta.dropwizard.resources.console.queue: DEBUG
#        ru.taskurotta.service.hz.storage: DEBUG
    file:
        enabled: true
        archive: true
        currentLogFilename: ./target/logs/service.log
        archivedLogFilenamePattern: ./target/logs/service-%d.log.gz
        logFormat: "%-4r %d [%t] %-5p %c - %m%n"

http:
    rootPath: "/rest/*"
    gzip:
        enabled: false
    requestLog:
        file:
            enabled: false
            archive: true
            currentLogFilename: ./target/logs/requests.log
            archivedLogFilenamePattern: ./target/logs/requests-%d.log.gz


#config can be extended here
#http://dropwizard.codahale.com/manual/core/#configuration-defaults